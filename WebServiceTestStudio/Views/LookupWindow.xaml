<Window x:Class="WebServiceTestStudio.Views.LookupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:WebServiceTestStudio.Views"
        Title="Lookup Window" Height="600" Width="600">
    <Window.InputBindings>
        <KeyBinding Command="{Binding InvokeCommand}" Gesture="F5"/>
    </Window.InputBindings>
    <Grid>
        <views:ParametersView DataContext="{Binding ParametersViewViewModel}" HorizontalAlignment="Stretch"></views:ParametersView>
    </Grid>
</Window>
